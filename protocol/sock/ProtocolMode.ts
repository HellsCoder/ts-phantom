/**
 * Режим протокола. Если установлен CLIENT - значит этот экземпляр протокола работает в режиме клиента, если SERVER - в режиме сервера
 */
export enum ProtocolMode {
    CLIENT,
    SERVER
}